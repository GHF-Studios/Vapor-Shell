[package]
name = "vapor_shell"
version = "0.1.0"
edition = "2024"

[dependencies]
rhai = { version = "1.23", features = ["sync"] }
rustyline = "14.0"
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }

